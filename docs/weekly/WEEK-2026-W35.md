# Week 2026-W35 — Plan

_Generated 2026-08-24 (Monday) by automotive-skills-daily-standup (PLAN mode)._

## Recap — past 7 days

```
74d017c auto(triage): twelve issues audited, stale rule misfires on two finished issues
47fc282 auto(release): v2026.08.W34 snapshot, chain breaks zero, autosar first pass
dd687bd auto(docs): W34 changelog roll, two reviewer stubs expose check-count drift, STATUS regen
15d4c8b auto(polish): #51 autosar-bsw-config gains sample input, drops two silent bugs to issues
ec8fc8e auto(polish): fix #53 fmeda TSC reader, repo chain breaks now zero
5fa7109 auto(polish): #46 chain-contract audit lands, finds silent fmeda-to-TSC break (#53)
547ced0 auto(plan): W34 descopes #46 after five carries, opens autosar and v&v targets
```

**W34 landed all three targets — the first clean week since W27, and the descoping worked.** #46,
carried five consecutive weeks with zero commits, shipped on the Tuesday it was slotted first,
because the plan cut its scope to builder-to-builder reads instead of carrying the original
76-pair DoD a sixth time. It immediately paid for itself: the audit found the silent
fmeda-to-TSC break filed as #53, which was fixed the same week, taking repo chain breaks to zero.
The scoping lesson is now evidenced, not asserted — **descope and slot first beats carry**.

One target under-delivered in a useful way. #52 (traceability-matrix, v&v) does not appear in the
commit log above; #51 (autosar-bsw-config) consumed two polish days rather than one, because the
pass surfaced two real defects — **#54 and #55** — that had to be written up rather than swallowed.
That is the right trade and the intended shape: polish passes that find defects file them as
issues rather than repairing them inline. But it means **v&v's first pass did not begin**, and the
W34 domain-spread claim of "half the four-domain gap closed" is currently half-true.

The two new issues are also the first genuinely-open work items filed since #46. Sunday's triage
confirmed the entering position: **12 issues open, 10 DoD-met, only #54 and #55 genuinely open.**

## This week's targets (3)

Three targets for three POLISH days, holding W34's arrangement. Priority order applied to the
**DoD-open** subset only — for the second week running, filling the table from the raw open list
would re-target ten finished issues. Rule (a) supplies two targets (#54, #55); rule (b) is skipped
(0 orphan builders, alias registry honored); rule (c) fills the third slot.

| # | Issue | Skill(s) | Domain | Definition of done |
|---|---|---|---|---|
| 1 | [#54](https://github.com/jherrodthomas/automotive-skills-suite/issues/54) (carried from W34) | autosar-bsw-config-builder + reviewer | autosar | `build_memory_map` accepts and writes `memory_layout`; `build_post_build_variants` aggregates `post_build_variant` from `module_parameters`; reviewer `check_memory_allocation` (C005) no longer returns NA on supplied input; `check_nvm_alignment` (C006) filters a field that actually carries a module name. Verified end-to-end against the committed `examples/sample_input.json` — a cell scan for `RAM_BSW` and `CAN1` must hit. |
| 2 | [#55](https://github.com/jherrodthomas/automotive-skills-suite/issues/55) (carried from W34) | 5 remaining autosar archives (adaptive-app + rte-mapping pairs, bsw-config reviewer) | autosar | Byte-identical 5,782-byte `recalc.py` restored in the remaining 5; each verified to import cleanly. `## Skills inventory` heading corrected in the same 5. Explicit decision recorded on `scripts/__init__.py`. Repo-wide re-scan confirms **152/152** on recalc size and **0/152** on the heading. Each of the 5 archives smoke-tested after repacking. |
| 3 | [#56](https://github.com/jherrodthomas/automotive-skills-suite/issues/56) (new) | sysml-block-diagram-builder | sysml | Frontmatter validated, generator smoke-tested from its own sample input (written if absent), every sheet name confirmed Excel-legal, `recalc.py` size confirmed against the repo norm, reviewer pairing confirmed by actually opening a builder output, polish log + `examples/sysml-block-diagram-builder/README.md` written from the verified run. |

Slotting: **Tue -> #54. Wed -> #55. Thu -> #56.**

#54 goes first because it is the only HIGH-severity correctness defect on the board and because its
second symptom is the dangerous one: the dropped input **launders into a passing audit**. An
analyst who supplies four memory regions is told C005 is `NA — No memory regions defined` on a
Mandatory check. A tool that silently reports the user's own input as absent is worse than a tool
that errors, and that outranks everything else here.

#55 is second because it is mechanical, fully specified, and touches five archives — the kind of
work that expands to fill whatever day it is given and should therefore not be given the last one.

### Why v&v (#52) is not in this table

#52 was slotted Thursday of W34 and did not start. The honest reason is that #51 overran, not that
#52 was mis-scoped, so it does not need the #46 treatment. But re-slotting it this week would mean
choosing four targets' worth of work for three days again — the exact failure W34 was built to
avoid. **#52 is deferred to W36 with its DoD unchanged.** If it slips a second time without an
overrun to explain it, that is the signal to descope it, and this note is the marker to check.

## Domain-spread check

Targets span **autosar (x2) and sysml**. Doubling down on autosar is deliberate: both items are
confirmed defects in a domain whose first pass exposed them one week ago, and leaving a
launders-into-passing-audit bug open to preserve domain variety would be optimizing the wrong
number. sysml takes the third slot and opens the first pass on one of the **two last never-polished
domains**.

| Domain | Builders | Last touched | Status after W35 (if targets land) |
|---|---|---|---|
| autosar | 5 | mixed (bsw-config fresh) | defects closed, batch consistency restored |
| sysml | 4 | 2026-05-01/02 | **first pass begun** |
| mbse | 3 | 2026-05-01/02 | still unworked -> W36 |
| v&v | 5 | 2026-05-02 (all) | still unworked -> W36 (#52 deferred) |

After W35, **mbse is the only domain never entered into a weekly table**, with
`mbse-system-context-builder` (8,565 bytes) the recommended W36 lead alongside the deferred #52.

## Notes

- STATUS regenerated via `scripts/regen_status.py`: **76 builders / 76 reviewers / 100% paired**
  (2 alias pairings), red 0 / **yellow 66** / green 10. Yellow rose 61 -> 66 and green fell 15 -> 10
  since 2026-08-17 purely from W33's commits aging past the 30-day window. **No skill regressed.**
  Sixth consecutive run making this observation: the flag measures cadence, not health, and one
  skill per polish day cannot hold 76 builders inside a 30-day window. It will oscillate forever
  until the window widens to 90 days or the flag is dropped — see standing item 6.
- The stale root-owned `/tmp/automotive-work` tree bit again, costing one aborted bash call. This is
  the **third consecutive occurrence**. Today's clone went to a timestamped sibling as before.
  Recommend the task file's fixed `WORK` path become timestamped so `rm -rf` cannot fail the run.
- Polish-log coverage: **26 of 76 builders (34%)**, up one on W34's #51 pass. Example READMEs: 46
  directories.
- #56 went out with `weekly-target` + `sysml` and **no type label** — fifth consecutive week
  blocked on the same unmade decision (standing item 3).

## Standing items for the human

1. **Close #43 through #52** — ten issues, all DoD-met with evidence cited in
   `docs/triage/2026-08-23.md`. Fifth consecutive run raising this. It is the single thing making
   the open count unreadable and it is what forces this plan to hand-filter the priority-(a) rule
   every Monday. #54, #55 and #56 are the only three that should be open.
2. **The 30-day stale rule is now actively misfiring.** Last Sunday it posted "closing in 7 days
   unless updated" on issues whose work shipped in `v2026.08.W33`. The 2026-08-19 deadline set two
   weeks ago passed with no action. Either close the ten above or amend the rule to skip DoD-met
   issues — it is currently generating misleading warnings on finished work.
3. **Add a `polish-target` label** (or accept `weekly-target` as the type). Fifth week blocked;
   #48-#52 and now #56 all ship with a domain label and no type label.
4. **Delete the duplicate `vv` label** — genuine collision with the in-use `v-and-v` in the domain
   namespace; a future run could plausibly pick either. Confirm unused first.
5. **Decide whether #46 closes or re-scopes.** Its DoD was narrowed in the W34 plan to
   builder-to-builder reads and static analysis only, so **column-layout drift remains uncovered**.
   Closing it as-is is defensible; so is a successor issue. It should not sit ambiguous.
6. **Sign off (or reject) the freshness rule change** — outstanding since W30. See the yellow-count
   note above.
7. **Publish the pushed tags** — v2026.07.W29 through v2026.08.W34 are pushed with no GitHub
   Release object (by design; autonomous runs never publish).
8. **The vanished-issues pattern #33-#42** — flagged W30, still unexplained, no new evidence.
